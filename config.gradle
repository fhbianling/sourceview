ext {
    baseConfig = [
            group: 'com.github.fhbianling'
    ]
    versionConfig = [
            plugin: "0.0.3",
            core  : "0.0.2"
    ]
    publishVersionConfig = [
            plugin: "0.0.3",
            core  : "0.0.2"
    ]
    artifactConfig = [
            plugin: "source-plugin",
            core  : "source-core"
    ]
}